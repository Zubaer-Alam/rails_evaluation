<div class="container">
  <div class="row justify-content-center">
      <%= simple_form_for brand, html: { class: "card m-2 p-3", style: "width: 100%;" } do |f| %>
        <% if @brand.errors.any? %>
          <div class="alert alert-danger">
            <%= @brand.errors.full_messages.to_sentence.capitalize %>
          </div>
        <% end %>

        <%= f.input :name, label: "Brand Name", input_html: { class: "form-control", autofocus: true } %>
        <%= f.submit "Submit", class: "btn btn-success mt-3" %>
        <%= link_to "Cancel", brands_path, class: "btn btn-outline-danger mt-3 mr-2" %>
      <% end %>
  </div>
</div>
