<main class="container">
  <h1>Brands</h1>
  <%= turbo_frame_tag "first_turbo_frame" do %>
    <div class="header">
      <%= link_to "Create Brand", new_brand_path, class: "btn btn-primary" %>
    </div>
  <% end %>

  <%= render @brands %>
</main>