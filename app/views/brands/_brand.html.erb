<%= turbo_frame_tag brand do %>
  <div class="brand">
    <%= link_to brand.name, brand_path(brand), data: { turbo_frame: "_top" } %>
    <div class="brand__actions">
      <%= button_to "Delete",
                    brand_path(brand),
                    method: :delete,
                    form: { data: { turbo_frame: "_top" } },
                    class: "btn btn-danger" %>
      <%= link_to "Edit",
                  edit_brand_path(brand),
                  class: "btn btn-danger" %>
    </div>
  </div>
<% end %>